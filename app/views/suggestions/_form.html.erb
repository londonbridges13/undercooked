<%= simple_form_for(@suggestion) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :rejected %>
    <%= f.input :reason %>
    <%= f.input :evidence %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
